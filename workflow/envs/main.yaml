name: rindti_clean

channels:
  - conda-forge
  - bioconda
  - pytorch #1
  - nvidia

dependencies:
  # --- Core ---
  - python=3.9
  - snakemake
  - snakemake-minimal
  - pyyaml

  # --- PyTorch + GPU ---
  - pytorch=2.4
  - pytorch-cuda=12.4
  - torchvision
  - torchaudio

  # --- Scientific Python ---
  - numpy
  - pandas #4
  - scipy
  - scikit-learn
  - statsmodels

  # --- Plotting ---
  - matplotlib
  - seaborn

  # --- Chemistry / ML utils ---
  - rdkit
  - pytorch-lightning #3
  - tensorboard
  - torchmetrics
  - xgboost

  # --- Utility ---
  - requests
  - tqdm
  - joblib

  - pip:
      - torch-geometric #2
      - glycowork # exclude
      - gdown # exclude
